import 'package:flutter/material.dart';

class AllCycleScreen extends StatelessWidget {
  const AllCycleScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Home"),
      ),
    );
  }
}
